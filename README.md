# Redirect(リダイレクト)の勉強

## リダイレクトとは
  -- サイトにアクセスしたユーザーをアクセスした URI とは別の URI にアクセスさせること。
  -- 今回の実装では、POST でのアクセスを GET へのアクセスへリダイレクトする。
  -- このリダイレクトには、HTTP のステータスコードの300番台を利用する。

## 今回行う設計は下記の3つ
  -- UI(ユーザーインタフェース）設計
  -- URI(Uniform Resource Identifier) 設計
  -- モジュール設計

